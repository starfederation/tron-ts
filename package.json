{
  "name": "tron-ts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "bench": "bun run bench/geojson.ts",
    "update:tron-shared": "git -C tron-shared fetch origin && git -C tron-shared checkout origin/main"
  }
}
